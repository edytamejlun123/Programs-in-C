#include <stdio.h>
#include <stdlib.h>

char * foo(char * napis1, char * napis2)
{
    return 0;
}
int main()
{
    char * n1 = "Edyta";
    char * n2 = "PS";
    printf("%d", foo(n1,n2));
    return 0;
}
